<h1>Events List</h1>
<ul>
	<% @events.each do |event| %>
	<li>
		<%= event.title %>
		<%= link_to 'Edit', edit_event_path(event) %>
	</li>

	<% end %>


</ul>

<%= link_to 'Create Event', new_event_path %>
<br/>
